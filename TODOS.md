# TODOs

## Master Plan

> **Always look here first for project context.** The master development plan covers all phases:
> `~/.claude/plans/foundry-master-plan.md` (written 2026-09-18 for the v0.3.x arc; thesis: the disciplines are built, the evidence that they run is not)
>
> The original, `~/.claude/plans/i-came-across-this-keen-gadget.md`, was deleted in the plans purge and transcript recovery on 2026-09-17 found no surviving copy. The replacement is not a reconstruction of it: it was written fresh from the shipped suite, this index, the public surfaces, and the two live plans. Phases 0 through 17 below are the record of the arc the lost file covered.

> **Reading this index.** It is not one list and the last line is not the newest plan. Entries run in two blocks: the first ends at 2026-09-23 and holds the current design, accessibility and skills-analysis work; the second, further down, holds Phases 13 through 24 and ends at 2026-09-18. Sort by the date on the entry, never by position, and never reorder them to fix this: the written order is a record.
>
> **What is live, in the order to take it**, as of the 2026-09-24 reconcile: the domain cutover below, which is a user-facing break and needs Craig's dashboard step first; then units D, E and F from the 2026-09-22 audits, in that order (E before F, they collide on the hero grid); then the skills-analysis and prompt-audit remediations; then Phase 19, `~/.claude/plans/foundry-phase-19-gates-prove-they-run.md`, which is the v0.3.x arc's centerpiece and its most valuable unblocked unit, but not its most urgent. Phase 18 is blocked on PostHog access and Phase 24 is conditional on 18, so neither is startable.
>
> This paragraph is hand-maintained and nothing fails when it goes stale. It named Phase 19 as the frontier from 2026-09-18 and was wrong from 2026-09-21, when a different stream of work started. Whoever closes a unit updates it.

> **HIGHEST PRIORITY, and it is not cosmetic: the domain cutover.** `fly.toml`'s `SITE_ORIGIN` still pins `https://foundry-skills.fly.dev` while the site's canonical host is `foundry.thehorizonfoundry.com`, and `app/unlock/actions.ts` builds `emailRedirectTo` from `SITE_ORIGIN`. So anyone who signs in at the custom domain is sent a magic link to the .fly.dev host, lands their session cookie there, and finds their report history dead on the primary domain. It has been open since the 2026-07-17 launch. **Craig's step comes first** (Supabase Auth -> URL Configuration: add `/auth/confirm` for foundry., www. and the apex, no wildcard), then the agent flips `SITE_ORIGIN` and the canonical and deploys. Flipping first breaks it harder. Full detail in the Pre-publish section below.

### Phase Plans

- [x] Phase 0: Bootstrap repo from scaffold (master plan)
- [x] Phase 1: Skill + report schema (master plan)
- [x] Phase 2: First run: first full application audit (master plan)
- [x] Phase 3: Docs, marketing site, deck, /reports, deploy (master plan)
- [x] Phase 4: Grow into a skills suite, doc-set spec, BRAND, seven skills, /skills site (master plan)
- [x] Phase 5: Foundry rebrand -> `~/.claude/plans/foundry-rebrand.md` (plan file since deleted)
- [x] Phase 6: Rename behind-the-build to document (conversational; see PROMPTS Phase 6)
- [x] Phase 7: Add foundry, the one-command pre-ship conductor (conversational; see PROMPTS Phase 7)
- [x] Phase 8: document idempotent + public/internal modes; foundry check/prepare; /pr + /ship split; public-adoption + safety pass; npx install; blocker pass (conversational; see PROMPTS Phases 8-11)
- [x] Phase 12: Supabase magic-link auth for /reports (+ PostHog, foundry init/list) -> `~/.claude/plans/foundry-reports-supabase-magic-link.md` (plan file since deleted) (merged PRs #20-#21, live)
- [x] 2026-07-13 Phase 12.5: Consolidation 11 -> 9 skills (cut resume, merge doc-drift into document, keep brand-voice) -> see NOTES 2026-07-13 (merged PR #29, live)
- [x] 2026-07-13 **Phase 12.9: Pre-public skills hardening** (merged PR #30, deployed 2026-07-13) -> `~/.claude/plans/okay-take-your-critique-clever-gadget.md` (plan file since deleted) (ABSORBS the old 12.9 quality pass AND the 12.10 lifecycle expansion into one everything-before-flip phase, per Craig. Covers: 2 launch-blockers; production-audit conditional dimensions + verdict scope qualifier + severity/evidence-class depth; full scaffold profiles; instrumentation multi-actor + governance; phase-plan honest outcomes + git reconcile; foundry project-specific policy gates + init/list moved to production-audit; document conditional showcase + intent preservation; mobile heuristics + device matrix; brand-voice approved-inputs + clarity-first; security-earlier; artifact-value + deterministic enforcement; and ALL marketing surfaces reconciled. Positioned as delivery integrity, five promises. Workstreams A-N in the plan.)
  - superseded plan files (all live items folded into the plan above; doc-drift-authority + resume already DONE via 12.5): `~/.claude/plans/foundry-skills-quality-pass.md` (plan file since deleted), `~/.claude/plans/foundry-phase-12-10-lifecycle.md` (plan file since deleted)
- [x] 2026-07-14 Phase 12.10: Positioning copy, "what you tune vs what's fixed" + "where Foundry fits" (outer-loop / composes with inner-loop skills) across PRODUCT/README/homepage/skills/deck/skill-cards (conversational; see PROMPTS Phase 12.10 + NOTES 2026-07-14). From colleague feedback ("can I tune what is production ready, or is it your own recipe?" + "have you seen Matt Pocock's work?"). Copy-only; deployed for reviewer eyes pre-flip. (closed: entry says deployed)
- [x] 2026-07-16 Phase 12.11: Clarity pass, make the copy live up to BRAND.md's plain-voice standard across every user-facing surface (conversational; see PROMPTS Phase 12.11 + NOTES 2026-07-16). From a design/product friend's "too dense, CEO language" read. Includes the brand-voice additive reframe and the /skills tile arrow-implies-order IA fix. Copy-only; deployed. (closed: entry says deployed)
- [x] 2026-07-16 Phase 12.12: Report reads as a triage tool, not a wall of text (ReportView redesign, persona = time-poor senior/principal eng or product lead; conversational, see NOTES + DESIGN 2026-07-16). Top scorecard (verdict + severity mix + gate status + not-assessed in one glance), strengths collapsed to headlines, repeated dimension cards to one line, mechanical sweeps compacted to tail. Deployed. (closed: entry says Deployed)
- [x] 2026-08-14 Phase 13.0: Self-audit as public proof, two examples + the "self-audit tracks the suite" invariant -> `~/.claude/plans/foundry-phase-13-self-audit-public-proof.md` (plan file since deleted) (regenerate Foundry's own audit under the current method, publish it public alongside a relabeled illustrative Perch) (closed: RESOLVED 2026-08-14 per sub-entry; both audits regenerated and shipped in v0.2.0)
  - RESOLVED 2026-08-14 on the feature-design branch: regenerated as `reports/foundry-2026-08-14.json` (with perch-2026-08-14). Historical context: the published self-audit was stale: `reports/foundry-2026-07-16.json` carried `auditedCommit: 7d85850`, many commits behind main. Workstream J deliberately did NOT regenerate it (see that plan's amended step 7): its 21 findings are all application-level and J changed only skill prose, the Makefile's cmp guards, and CLAUDE.md, so a regeneration would have reproduced substantially the same report at a full audit's cost. PR #6 (display face) and PR #9 have both merged, so the surface this phase should audit is now settled.
- [ ] 2026-07-25 Gate-regression harness (v0.2.0 candidate: deterministic skill-invariant lint in make validate + model-exercised scenario suite at requalification; sibling of the seeded-target eval in Backlog) -> `~/.claude/plans/foundry-gate-regression-harness.md` (plan file since deleted)
- [x] 2026-08-13 Version marker + run-time version check (Craig-approved 2026-08-13; rides alongside the v0.1.1 closing session, marker amended into workstream A) -> `~/.claude/plans/foundry-version-check.md` (plan file since deleted)
- [x] 2026-08-13 feature-design, DECIDED 2026-08-13 (Craig): standalone skill, the suite's eleventh, with a sixth promise and full public-surface documentation; on completion regenerate BOTH public audits (Foundry self-audit + the Perch example). Execution plan -> `~/.claude/plans/foundry-feature-design-skill.md` (plan file since deleted) (definition lives in `~/.claude/plans/foundry-feature-design-proposal.md`, plan file since deleted; both supersede the Backlog draft on the proposal branch). BUILT on `feat/feature-design-skill` 2026-08-14 (skill + two-review hardening + fresh-agent dogfood + both audits regenerated + whole-surface sweep); checkbox closes when the PR merges.
- [x] 2026-07-16 Phase 12.13: Pre-publish cleanup from two audits (adversarial 4-agent sweep + external reviewer; conversational, see NOTES + PROMPTS 2026-07-16). SHIPPED: removed tracked `.impeccable/` critique logs (a Phase-13 plan gap), 3 report edge-case bugs (empty-findings, posture-fallback, specimen assessedScope), BRAND "skill is a command" contradiction, install-command drift, frame nine-vs-eight drift, stale CLAUDE component registry, mobile (no-autofocus-on-coarse + Pointer Events) and instrumentation (durable outbox for un-loseable events + funnel-not-universal) technical corrections. The external audit was ~90% stale (all 9 "blockers" already fixed in 12.9/12.10). Remaining pre-flip items below. (closed: entry says SHIPPED; remaining items tracked in the sections below)

- [x] 2026-09-21 Mark swap to design kit 1.3 (the mark's 2026-09-19 revision across icon, chrome lockup, deck, and share card; the kit's `web/foundry/` favicon set, which is NOT the studio set; a built-output sweep so no retired path survives; two decisions it must make, the manifest product name and bone vs amber-outline share image) -> `~/.claude/plans/foundry-twin-ridge-mark-swap.md` (closed: merged 2026-09-22 as PR #26)
- [x] 2026-09-21 Brand inheritance cleanup (this repo stops being the declared owner of the Horizon Foundry palette; the four brand values are inherited from the parent BRAND.md and the kit, `command` and `brand-slate` stay as aliases but name their parent values, and the stale "slate + white" brand description goes). Docs and comments only, no token values change -> `~/.claude/plans/foundry-brand-inheritance-cleanup.md` (closed: merged 2026-09-22 as PR #27)
- [x] 2026-09-22 Hero motion without a glow (cut the ambient amber glow and the spotlight the anti-goals ban, replace them with the grid reading the pointer as an instrument, give motion its tokens, and measure the kicker's contrast rather than assuming it) -> `~/.claude/plans/foundry-hero-motion-without-a-glow.md` (closed: merged 2026-09-22 as PR #29; the motion system's remaining truth defects were fixed in PR #31, tracked under the audit-remediation entry above)
- [x] 2026-09-22 Audit remediation (every P1 and P2 from both 2026-09-22 audits plus the eleven findings the hero change introduced, in six sequenced units: the motion system, the hero band, what ships vs what source says, the deck, the accessibility floor, delivery performance) -> `~/.claude/plans/foundry-audit-remediation.md` (CLOSED 2026-09-23: units A, B and C merged as PR #31 after four adversarial review rounds; the band decision it carried is resolved, Craig cut the crosshair; units D, E and F are split into their own plans below.)
- [ ] 2026-09-23 Unit D, The deck stops mis-navigating (7 of 9 dots activate the wrong slide through overlapping hit areas, a heading-level skip, no touch gestures, and every control under 24px) -> `~/.claude/plans/foundry-unit-d-deck-navigation.md`
- [ ] 2026-09-23 Unit E, The accessibility floor (content lost at 175% text zoom, /reports with no h1 signed out, /skills/[slug] overflowing at 320px, three mislabelled landmarks; do BEFORE unit F, they collide on the hero grid) -> `~/.claude/plans/foundry-unit-e-accessibility-floor.md`
- [ ] 2026-09-23 Unit F, Delivery performance (13 content routes uncached SSR, posthog-js in every first-load bundle, a stray font preload, the standalone bundle carrying the project root; gated: do unit E first) -> `~/.claude/plans/foundry-unit-f-delivery-performance.md`
- [ ] 2026-09-23 Prompt-audit remediation (six fixes from the 2026-09-23 `/claude-api prompt-audit` of the eleven skills: production-audit claims `make validate` enforces two hard rules that nothing enforces in an adopter install; its dimension subagents are asked to rate against a rubric they are never handed; plus four smaller cuts; five flagged items carried as owned open questions) -> `~/.claude/plans/foundry-prompt-audit-remediation.md`
- [x] 2026-09-23 Raw analysis of the eleven skills (usefulness, effectiveness, value, correctness: a correctness sweep, real-use evidence from transcripts and artifacts, a value critic, a deep flagship critique, and eleven cold runs including a full production-audit scored for recall; a scorecard with a verdict per skill; actionable findings written back as indexed units) -> `~/.claude/plans/foundry-skills-raw-analysis.md` (closed 2026-09-23: completed; scorecard in its Results, evidence in `foundry-skills-raw-analysis-evidence/`, follow-ups in the remediation plan below)
- [ ] 2026-09-23 Skills-analysis remediation, six units (A: stop shipping wrong advice in mobile, instrumentation, the version check, disclosures and the BRAND contract, with no decisions needed; B: the flagship can say "not safe", since seven synthetic reports including safe-to-ship with an unverified Critical pass both validators today; C: the cross-skill records connect; D: the public proof tells the truth; E: packaging and triggering, including feature-design never installed; F: site defects found on the way) plus six open questions owned by Craig -> `~/.claude/plans/foundry-skills-analysis-remediation.md` (Unit B gated: Q2 and Q3, the verdict's burden of proof and the risk test; Unit A can start now)

### Pre-publish, still open (from the 2026-07-16 audits)

- [ ] 2026-07-17 **[Security]** **[HIGHEST PRIORITY]** **The domain cutover** (see the call-out at the top; this entry is its home). Two steps, in this order, and the order matters because doing the second first breaks sign-in harder than leaving it alone:
  1. **Craig, in the Supabase dashboard** (Auth -> URL Configuration): confirm the redirect allowlist carries NO wildcard, and add `/auth/confirm` for `https://foundry.thehorizonfoundry.com`, `https://www.thehorizonfoundry.com` and the apex.
  2. **Then the agent**: flip `SITE_ORIGIN` in `fly.toml` from `https://foundry-skills.fly.dev` to the canonical host, confirm `app/unlock/actions.ts` builds `emailRedirectTo` from it, deploy, and verify a real magic link end to end on the custom domain before calling it done.
  Open since the 2026-07-17 launch. What it costs while open: every sign-in from the canonical domain mails a link to the .fly.dev host, so the session lands on the wrong origin and report history reads as empty on the domain the site actually advertises. Also closes SEC-01's remaining half (the no-wildcard confirmation) in the Backlog.
- [x] **[Blocker]** `SECURITY.md` created (PR #58): private disclosure via GitHub's built-in vulnerability reporting (Security tab), so no email is published and it does not depend on the Workspace mailbox. Enable Private Vulnerability Reporting in repo settings at the flip.
- [x] **[Release]** CI on main (PR #58): `.github/workflows/ci.yml` runs typecheck/lint/build/`make validate` on PRs+pushes to main. First run GREEN (SHIP-01 closed). Once public, main is not an ungated install channel.
- [ ] **[Decision]** The proof is one fictional app (Perch) staged three ways; the frame's own success-measure entry is blank. Highest-leverage pre-launch move: run `/production-audit` on one real public repo and publish it alongside Perch. (Craig's call)
- [ ] **[Decision]** The "Horizon Foundry suite/project" chrome on a solo v0.1.0 repo: earn it or soften to a maker's toolkit for launch. (Craig's call; HF is real, this is launch-optics)
- [ ] **[Polish]** Minor positioning: /skills leads with the abstraction vs the concrete hero line; the "not product strategy, not org facilitation" negations; the eight-epigraph /behind deck (Aristotle twice)
- [x] 2026-07-29 **[Brand/cross-project]** Display face PR #6 (Archivo Black) MERGED to main (31d2d13). Historical: was open awaiting Craig's merge. Owned by the Horizon-Foundry studio-site workstream (that brand decision cascades here as the Foundry companion PR; the display face churned Big Shoulders -> reversed -> Archivo Black, so confirm settled before merge). Tracked in the HF TODOS launch blockers; noted here so a Foundry-only session sees the open PR. The v0.1.1 doc sweep + regenerated self-audit should run AFTER this merges, to reflect the final surface.
- [ ] **[Skill]** document public mode should curate specific PRODUCT.md sections, not assume the whole file is publish-safe; reconcile "a broken mobile flow is automatically High" with the earned-severity rubric
### From the 2026-07-16 fresh external audit (the substantive, queued items)

That audit was much fresher than the prior one; its #1 "blocker" (retired /commit, /doc-drift, /resume, /behind/{prompts,notes,friction} pages "still live") was FALSE (all 404, verified) and internal links to them are clean (verified). Surgical copy fixes it got right are DONE (Terminal "provisional verdict" -> "read-only release-gate scorecard"; Perch "exactly-once" -> "effectively-once"; scaffold "production-ready" -> "in the shape its profile requires"; "guardrails actually run" softened; "external critiques judged the method sound" -> honest; "Your reports"/"Yours are waiting" -> no implied self-service hosting; PA "six apply to every product" -> applicability-resolved for all 11; brand-voice glossary "bug" rule scoped to report terminology). Remaining, genuinely worth a dedicated skill-precision pass:

- [ ] **[Skill]** `--runtime` enum rename (deferred): the SKILL prose now scopes `--runtime` to a browser pass and leaves real-device/infra/capacity/migration `not-assessed`, so the overclaim is fixed in behavior. The remaining option, renaming the `static-plus-runtime` assessedScope enum to something narrower, is a schema change held for the next `schemaVersion` bump.
- [x] 2026-07-16 **[Skill/Consistency]** Second external audit (2026-07-16) consistency pass, one PR (PR7). Fixed the drift between #42-47's stronger definitions and the surfaces that lagged: verdict terminology normalized to "ready to ship, risks noted" everywhere (lib/skills.ts, PRODUCT.md x2, PA SKILL); PA description/table/Phase-0 aligned to "every applicable dimension among the eleven"; PA mobile/instrumentation gate weighting now follows the required/optional/waived release policy instead of "not optional/always caps"; stray Perch "exactly-once" -> "effectively-once" with a delivery-semantics note; phase-plan Red Flag scoped to meaningful handoffs; /skills "guardrails actually run" softened to explicit-and-hard-to-skip + schema/CI for true invariants; scaffold profile table row reflects optional PROMPTS/FRICTION + license-as-appropriate. Item 7 ("Yours are waiting") and the "six core apply to every product" / old-Perch claims were already fixed (verified, stale in the audit).
- [x] 2026-07-16 **[Decision/Bug]** /behind/product PUBLICLY rendered the raw frame (found live 2026-07-16): security-frame enumeration, `(assumed, unconfirmed)` hedges, two open `(finding: not yet declared)` markers, violating both `document public`'s rule and PR4's new section-level rule. RESOLVED PR5: Craig chose "render truth sections only"; added `stripSection` to lib/docs.ts and the public product tab now strips `## The frame`, leaving present-tense truth. The frame stays in the gated internal portal (the repo).
- [x] **[Skill]** Public-doc projection rule: `document` now defines public-by-default vs private-by-default at the SECTION level, so "sanitized" is enforceable, not prose (a `(finding:`/`(assumed,` marker on a public page is the same failure as `NOTES.md` there). (Done PR4.)
- [x] **[Example]** Regenerate the Perch report under the CURRENT method: assess the applicable conditional dimensions (it has deploy/db/testing/ops/capacity surfaces, currently all excluded as "introduced after this run"), and fix the mobile ship-gate from "met" to at-risk/not-assessed (device checks are runtime; a static run cannot "meet" it). The example is the credibility asset and should demonstrate today's method, not July 12's. (Done PR2 #43: 11 dims assessed, 0 excluded, added OPS-01/DATA-01/TEST-01/SHIP-01/PERF-01, mobile gate -> not-assessed, stats 0/0/6/8/4.)
- [x] **[Skill]** Normalize the release-state contract across foundry + production-audit: add `not-assessed` to foundry check's scorecard states; split `blocked` (tooling failed) from `not-assessed` (lacked evidence); make gate optionality respect the policy (optional/waived) instead of mobile/instrumentation always capping; define `optional` behavior (assessed and reported, does not independently cap the verdict). (Done PR1 #42.)
- [x] **[Skill]** Separate gate failure from finding severity: a broken mobile flow can block a mobile-required launch without automatically being High. (Done PR1 #42.)
- [x] **[Skill]** Per-skill refinements: foundry (define optional; prepare may leave human verification outstanding); production-audit (real secret scanner not just grep; Common Mistakes should say Critical+High+verdict-driving; qualify "unpinned critical dependency") [PR1]; frame (success measure informs the instrumentation plan, is not automatically the activation definition); scaffold (testing/CI baseline in production-shaped; first-commit-on-default-branch conditional on a new repo; resolve the LICENSE-required vs LICENSE-where-one-exists contradiction; PROMPTS/FRICTION optional not universal); document (tighten truth-check scope from "every file touched/read" to docs + externally-meaningful contracts); mobile (a screenshot cannot confirm gestures/keyboard/focus, require an interaction pass; iOS+Android follow the declared support matrix); instrumentation (add a guardrail/countermetric field; qualify identify/alias as vendor-specific); phase-plan (do not require a plan for every routine PR, trigger at meaningful handoffs; add base commit/branch/verification state); brand-voice (machine-readable approval metadata in BRAND.md: Status draft|approved, approved-by, approved-on) [PR3 #44].
- [x] 2026-07-16 **[Decision]** Resolve the frame's own blanks (success measure, learn-before-investing) before publishing. RESOLVED 2026-07-16 (PR6): Craig confirmed both candidates, so entry 6 (a stranger completes one full `/production-audit` run on their own project) and entry 9 (does anyone beyond the maintainer run a skill in the first weeks after the flip) are now declared, the `(finding: not yet declared)`/unconfirmed markers removed, and the first-run findings closed with a dated resolution. Docs-only (the frame renders on no public surface after PR5), so no deploy. The frame skill's pre-flip gate is now satisfied.

- [x] 2026-07-17 Phase 13: Publish, LAUNCHED 2026-07-17 as `horizon-foundry/foundry` (fresh orphan repo, custom domain live) -> `~/.claude/plans/foundry-phase-13-publish.md` (plan file since deleted) (top section holds the remaining launch-tail checklist: Supabase allowlist + SITE_ORIGIN flip, www DNS, GitHub presentation, branch protection)
- [ ] Phase 14: Post-launch learning (the one lifecycle item that needs launched users, so it is correctly post-flip) -> seeded in the Phase 12.9 plan's workstream N; write its own plan file when Phase 13 completes. Did users reach the outcome, which assumptions died, expand/change/stop; closes the loop instrumentation opens. (2026-09-17 reconcile: the trigger has fired, Phase 13 completed 2026-07-17, and no plan file was ever written. Still genuinely blocked on launched users, so this is a live entry with an overdue artifact, not a missed close.)
- [x] 2026-08-14 Phase 15: release hardening, SHIPPED AS v0.2.0 on 2026-08-14 (there was never a v0.1.1 release; the roll-up absorbed this plan's merged workstreams plus the version marker/check, feature-design, and both regenerated audits; PR #14, tagged, released, deployed). REMAINING (the plan's 2026-08-14 amendment): workstream A leftovers and workstream I, tracked as the sub-entries below; kickoff "start the release-hardening leftovers (workstreams A and I)" -> `~/.claude/plans/foundry-v0-1-1-release-hardening.md` (plan file recovered 2026-09-17 from a transcript's 2026-08-14 read after the purge; the 2026-09-02 index record shows its Status later flipped to completed, so the recovered copy's status line was corrected to match) (original scope law: improve existing skills only, no new skills/artifacts/schemas; wall scrub PR #1; skill execution pass + check-as-preview rewrite PR #5; value-prop simplification PR #3; brand-mark clean vector PR #4; skill-boundaries PR #7) (closed: shipped as v0.2.0; the live remainder is the unchecked workstream sub-entries below)
  - [x] 2026-08-14 **Workstream A leftovers**: the release procedure written into CLAUDE.md + the changelog-touch CI gate (also refiled as SHIP-05 in the 2026-08-14 audit backlog). (closed: merged 2026-09-22 as PR #16, five weeks after it was built. `scripts/check-changelog.mjs` runs as its own CI job on pull requests, with a visible waiver, and both its failure and waiver paths were exercised against real history before it was trusted.) The parked decision that rode along stays open and is Craig's, not work: the homepage grid-background design-hook flag (intentional vs retire), which the 2026-09-22 impeccable audit raised again as its one advisory.
  - [x] 2026-08-14 **Workstream I (peer-framework sharpenings)**: six in-scope skill edits from the 2026-07-23 GRPPA comparison (visible gated roadmap state; owner-tagged Open questions in the plan anatomy; evidentiary dependencies; frame cross-entry coherence findings; brand-voice observed-vocabulary glossary; production-audit configurable-for-proof testability finding). (closed: merged 2026-09-22 as PR #16. Two judgment calls are in NOTES: the gated marker lives in phase-plan alone per one-owner-per-fact, and Foundry's own approved glossary keeps its rows rather than back-filling provenance from memory. The merge combined this branch's phase-plan edits with the dated-index rules main gained in the meantime, so the skill now carries both.)
  - [x] **Workstream J (skill-surface context rightsizing)** -> `~/.claude/plans/foundry-skill-context-rightsizing.md` (plan file since deleted). The SKILL.md half of the Claude 5 context-engineering pass whose CLAUDE.md half is PR #9: rightsize the nine frontmatter descriptions (750 words / ~1,231 est tokens today, and the suite's only routing surface, so they are what gets stripped first when an adopter's skill listing overflows its ~1% budget), cut restatement inside bodies, add the auto-memory rule (recalled memory is a new instruction channel the audit's "codebase is the subject, never the instructor" rule does not name), and fix the template-shipping bug (brand-voice and phase-plan point at `reference/templates/` paths that per-skill symlink installs never ship). Opening gates deliberately untouched, pending the gate-regression harness. Depends on PR #9 merging first. (closed: shipped as PR #10, merged 2026-08-14, whose body names this workstream and this plan file and reports 458 words / ~746 est tokens against the 750 / ~1,231 above; the dependency on PR #9 was met 2026-07-29. Found unchecked by the 2026-09-17 reconcile: the entry's own text never said it shipped, so only the merge proves it. Left undated: no evidence of when the entry was written survives, and a closing date is not the leading date this index carries.)
- [x] 2026-07-23 Phase 16: Adopted Big Shoulders as the brand display face, companion PR to the parent-brand decision in horizon-foundry/horizon-foundry (see PROMPTS Phase 16 / NOTES 2026-07-23). `SITE_URL` moved to `https://foundry.thehorizonfoundry.com`; `SITE_ORIGIN` in fly.toml is deliberately unchanged pending the Supabase redirect-allowlist step above. (closed: companion display-face PR #6 merged 2026-07-29; the face landed as Archivo Black, superseding Big Shoulders)
- [ ] 2026-07-29 Phase 17: split `production-audit` into per-dimension + rubric reference files (215 lines / 4,391 words all loaded at invocation; the fan-out should hand each subagent its own dimension file). Renumbered from 16 when the Big Shoulders companion PR claimed that number first. Deferred out of v0.1.1 by its scope law because it creates new artifacts; must ship inside `skills/production-audit/` on the bundled-schema pattern. Write its plan file when Workstream J closes. (2026-09-17 reconcile: Workstream J closed 2026-08-14, so this trigger has fired and the plan file is owed. Duplicated in the Backlog as the production-audit progressive-disclosure item; the two describe one piece of work.)
- [x] 2026-09-02 Phase Plans index backfill (apply the dated-index rules to this file; they shipped 2026-09-02 and this index predates them) -> `~/.claude/plans/foundry-phase-plans-index-backfill.md` (closed: merged 2026-09-17 as PR #20. The backfill shipped the work but left its own entry unchecked and its plan file at `Status: active`, which is exactly the index-versus-plan drift the 2026-09-02 rules named; both corrected by the 2026-09-17 reconcile.)
- [ ] 2026-09-03 Design-quality boundary (cut the `document` kicker mandate that impeccable bans; copy-provenance check in `brand-voice`; `not-assessed` exit in `production-audit` ui; optional `design:` row in `foundry check` citing impeccable) -> `~/.claude/plans/foundry-design-quality-boundary.md` (sub-plan of the workspace design-quality wave; no critic loop, no design tooling in Foundry)
- [ ] 2026-08-27 Skills pressure-test learnings, per-skill improvement items written back from a deep adversarial audit of a live household product (the skills ran as review instruments) -> `~/.claude/plans/foundry-savor-pressure-test-learnings.md` (orphan plan indexed by the 2026-09-17 backfill; plan Status: active, queued for a Foundry improvements PR)
- [ ] 2026-09-18 Phase 18, Read the verdict (answer the frame's entry-9 learn-before-investing threshold from `skill_version_check`, two months overdue; blocked on PostHog access for this project, and the answer gets written into the frame either way) -> `~/.claude/plans/foundry-master-plan.md`
- [ ] 2026-09-18 Phase 19, The suite proves its own gates run (skill-behavior tests asserting on the tool-call trace across two model families; absorbs the gate-regression harness and the seeded-target eval, which are one idea at two altitudes; promotes the degraded-run and evidence-validity invariants off prose) -> `~/.claude/plans/foundry-phase-19-gates-prove-they-run.md` (unit plan written 2026-09-18; the arc it belongs to is `~/.claude/plans/foundry-master-plan.md`)
- [ ] 2026-09-18 Phase 20, Close the coverage gap (the escalation-path data-layer/authorization lens plus the shared audit-stance convention, from the 2026-08-27 pressure test where a direct-read isolation test passed while the escalation path was open; the argued exception to "do not add more skills yet", shape audited by feature-design first) -> `~/.claude/plans/foundry-master-plan.md`
- [ ] 2026-09-18 Phase 21, The promise spine covers the suite (four of eleven skills keep no promise: scaffold, mobile, readout, brand-voice; grow the promises or restate the claim, plus the four-surface promise wording drift and the Big Shoulders vs Archivo Black CHANGELOG defect) -> `~/.claude/plans/foundry-master-plan.md`
- [ ] 2026-09-18 Phase 22, The design-quality boundary (already planned in full; cut the kicker mandate this suite currently requires and impeccable bans) -> `~/.claude/plans/foundry-design-quality-boundary.md`
- [ ] 2026-09-18 Phase 23, Progressive disclosure for the flagship (supersedes Phase 17 and its Backlog duplicate, which describe one piece of work; sequenced after Phase 19 because that is what would catch a silent behavior change) -> `~/.claude/plans/foundry-master-plan.md`
- [ ] 2026-09-18 Phase 24, Distribution (conditional: do not open until Phase 18 has an answer) -> `~/.claude/plans/foundry-master-plan.md`

## Launch sequence (do in this order)

The public flip now comes AFTER the reports auth (Craig, 2026-07-12) AND after
the skills quality pass (Craig, 2026-07-13: "make it better before we push it
public... make sure the skills and documentation are truly valuable to people
who come across them").

0. [x] **Pre-public skills hardening (Phase 12.9).** DONE: merged PR #30 and
   deployed to foundry-skills.fly.dev 2026-07-13. BUILT on branch
   `pre-public-hardening` (workstreams A-L + J all committed: launch-blockers,
   schema v3 scope-qualified verdict + conditional dimensions, per-skill
   honesty passes, foundry release policy, deterministic enforcement, and the
   delivery-integrity positioning across every surface; fresh-install sim +
   route-parity walk verified). Dogfood DONE 2026-07-13: `/frame` wrote and
   audited the suite's own frame into PRODUCT.md (entries 6 and 9 are blanks
   awaiting Craig), and the full `/production-audit` self-run validated clean
   against schema v3 (`tmp/audit-2026-07-13.json`: 0 critical, 0 high; verdict
   clear within assessed scope; findings folded back: CHANGELOG nine-skill fix,
   em-dash sweep; remediation backlogged under "From the 2026-07-13
   self-audit"). Remaining: PR + merge.
   Plan: `~/.claude/plans/okay-take-your-critique-clever-gadget.md`.

1. [x] **Supabase magic-link auth for /reports.** DONE: Phase 12, merged PRs #20-#21, live and verified across all four access cases. Anyone signs in by magic link and sees only their own reports (owner match); admin sees all; public example stays ungated. Only remainder: remove the old `REPORT_ACCESS_EMAILS`/`REPORTS_GATE_SECRET` Fly secrets if still set, folded into Phase 13 step 7. Full plan: `~/.claude/plans/foundry-reports-supabase-magic-link.md`.
2. [x] **Positioning copy (Phase 12.10).** Deployed 2026-07-14 for reviewer eyes before the flip: "what you tune vs what's fixed" and "where Foundry fits" (outer-loop / composes with inner-loop skills) across every surface. Answers a colleague's "is it your own recipe?" + "have you seen Matt Pocock's work?". Copy-only; repo stays private.
3. [x] **Publish (fresh-repo swap + flip public).** DONE 2026-07-17 (see Phase 13 in Phase Plans; launch-tail items remain there). Historical: full plan `~/.claude/plans/foundry-phase-13-publish.md`; at the time of writing the repo was still private, so every GitHub link on the live site 404d and `npx skills add` failed. Mechanism CHANGED (hardened 2026-07-13): NOT squash-in-place (GitHub keeps `refs/pull/*` refs that leak old history) but a fresh-repo swap: rename the current repo to `foundry-archive` (stays private), create a new `horizon-foundry/foundry` with one clean commit, verify, then flip public. Two hard gates (repo swap, visibility flip) + local-clone neutralization + archive guard + branch-protection + green-CI. IRREVERSIBLE / outward-facing: explicit go-ahead required.

## Backlog

### From the 2026-08-14 self-audit (safe to ship, 0 risks, 39 improvements)

Report: `reports/foundry-2026-08-14.json` (auditedCommit 81fec42). The one risk (the Next.js Server Actions DoS) was fixed in-branch before publication. The 39 improvements group into the report's remediation plan:

- [ ] **[Security/Testing]** Close the report-confidentiality seam before any private report lands: publish-time validate control, unexport getReport, first pinning tests (SEC-01, TEST-01)
- [ ] **[Ops]** Operations section plus one external signal: the runbook indexed below, an uptime check on /api/version, the request-stage failure log, the two documented recall paths (OPS-01, OPS-02, OPS-03, OPS-04, SHIP-04)
- [ ] **[Release]** Execute the release protocol: tags DONE at v0.2.0; the changelog CI gate and the named post-deploy checks landed 2026-08-14 (SHIP-05, and the procedure in CLAUDE.md names the checks). REMAINING: the main branch ruleset, and a deploy guard with a revision stamp (SHIP-01, SHIP-02, SHIP-03)
- [ ] **[Auth]** Pre-onboarding auth hardening before the first external report owner: interstitial confirm page, honest failure copy, signed-in /unlock redirect, pre-auth identity hygiene (REL-01, REL-02, CON-01, CON-02, SEC-02, SEC-03)
- [ ] **[A11y/UI]** Accessibility and polish pass, deck and scorecard focus management first (A11Y-01 to A11Y-05, UI-01 to UI-05)
- [ ] **[Decision]** Two remaining flagged by the sweep, Craig's call: trim the unread NOTES/PROMPTS/FRICTION Dockerfile COPYs (INF-03); repo description refresh to name the design-check promise. (The third, meta.version, was resolved at the v0.2.0 roll-up: both public reports stamped 0.2.0.)

### From the 2026-07-22 harness audit (deferred beyond v0.1.1)

- [ ] **[Eval]** Seeded-target eval for production-audit (v0.2.0 candidate): a fixture repo with planted true positives AND planted refutation-bait (findings that look real but are mitigated at another layer, i.e. the false-positive kill list as test cases); score recall on the plants and kill-rate on the bait. Turns "the audit works" from a claim into evidence. See the Phase 15 plan's kickoff decisions for context.
- [ ] **[Skill]** Post-ship closure artifact: a lightweight record of what was verified live after deploy (URL, checks run, what was not verified), consumable by ship-style workflows. The v0.1.1 release-safety dimension edit covers the audit side; this is the emitting side.
- [ ] **[Skill]** production-audit progressive disclosure (v0.2.0 candidate): the flagship is 2.5x the suite's median length with everything inline. Split the per-dimension specs, severity rubric, evidence classes, and kill list into reference files bundled next to the SKILL.md (the schema already ships that way), consumed by the Phase 2 subagents; the orchestrator's hot path shrinks and each fan-out agent loads exactly one dimension's spec.
- [ ] **[Skill]** Runtime skill machinery (v0.2.0 candidate, new artifact class so it needs its own decision): bundled scripts whose output is the next instruction (environment-aware setup, staleness checks computed rather than prose-directed) and edit-time hooks that enforce the report and doc invariants passively. Today the suite's deterministic enforcement is repo-side only (make validate, CI); a hook fires whether or not anyone invoked a skill.
- [ ] **[Portability]** Harness build step (v0.2.0+ candidate): the skills are authored for Claude Code with prose degradation paths for missing capabilities. A compile step (one source, per-harness builds: question mechanics, command prefixes, per-model corrections) is the durable version; prose portability is the v0.1.x answer.

### Suite hygiene (2026-07-25)
- [x] 2026-09-02 **[Docs]** **Backfill this file's own Phase Plans index.** The dated-index rules shipped 2026-09-02 apply going forward, and this index predates them: no entry carries a date, five entries whose text says SHIPPED or Deployed or RESOLVED are still unchecked, `foundry-v0-1-1-release-hardening.md` is `Status: completed` against an unchecked entry, `foundry-feature-design-proposal.md` is superseded with no annotation, `foundry-savor-pressure-test-learnings.md` is active and never indexed at all, and 8 of 14 referenced plan paths no longer exist on disk, including the master plan the file opens by telling readers to look at first. Its own unit of work: backfill only dates evidence supports, correct the checkboxes, annotate or retire the dead paths, and do not reorder history. The skill now states this migration posture so a mixed index is expected rather than a violation. (closed: done by PR #20, merged 2026-09-17. This Backlog line was a duplicate of the Phase Plans entry for the same unit, written the same day, and it survived the backfill because the backfill corrected the Phase Plans list and not its own duplicate down here. The Phase Plans entry is the one home for this fact; found by the 2026-09-17 reconcile, which had already missed it once.)

- [ ] **[Skill]** Usage-tracked skills registry: record per-skill invocation (hook-side counter is the likely mechanism) with a review cadence; a skill with no use in 60 days gets better trigger examples or a deprecation review. Dead skills are maintenance burden with no benefit.
- [ ] **[Process]** Periodic cross-model configuration review: hand the suite's standards plus one skill to a different model for critical review on a stated cadence; a different architecture has different blind spots and catches normalizations the primary model has stopped seeing.

### From the 2026-07-13 self-audit (Phase 12.9 dogfood)

The full `/production-audit` run on this repo: verdict clear within assessed scope (static), 0 critical, 0 high, 7 medium; report at `tmp/audit-2026-07-13.json`. Two refuted highs and five downgrades are recorded in the report's verification notes. CHANGELOG reconciliation and the em-dash sweep were fixed on the branch; the rest is backlogged here for an explicit pre-flip vs post-flip decision (SHIP-01 is the audit's top recommendation).

- [x] **[Release]** CI workflow on main: npm ci, typecheck, lint, build, make validate (SHIP-01; closed via PR #58, first run green)
- [ ] **[Release]** Extend `make validate` to gate SKILL.md content: frontmatter parses, mandatory gate paragraph present, no em dashes (SHIP-02)
- [ ] **[Testing]** Four pure-function gate tests: canView ownership matrix, slug traversal guard, meta.public gate, adminEmails parsing (TEST-01; ~1 hour, no mocking)
- [ ] **[Reliability]** AbortSignal.timeout on the Supabase fetch (all three client constructions); the confirm-failure logging half landed in Phase 13.2, the request-stage log is still open (refiled as OPS-02 in the 2026-08-14 audit) (REL-01, OPS-01, OPS-02)
- [ ] **[Security]** The pin landed in Phase 13.2 (`publicOrigin` prefers server-only SITE_ORIGIN); REMAINING: confirm the Supabase redirect allowlist carries no wildcard, which rides Craig's domain-cutover step (SEC-01)
- [ ] **[Perf]** `generateStaticParams` on /skills/[slug] + /example/[slug]; React.cache on getSessionUser (PERF-01, REL-04)
- [ ] **[A11y]** 16px unlock input at mobile widths; role=status on the copy confirmation; min target heights on SlideDeck prev/next + SignOutButton (A11Y-01, A11Y-02, A11Y-03)
- [ ] **[Ops]** Operations section in CLAUDE.md: rollback command, the two known auth failure modes and their checks, alerting confirmation, priority order (OPS-03, OPS-04, SHIP-04)
- [ ] **[Infra]** USER node in Dockerfile; `.env` in .dockerignore; digest-pinned base image (INF-01, INF-05, INF-02)

### Reports / auth

The auth model, stated plainly (Craig, 2026-07-12): the skills never require auth; sign-in exists ONLY to view your report history on the web; each report is visible only to the email it belongs to.

- [x] **`/foundry init`**: built 2026-07-13 (foundry SKILL.md). Records the operator email in `~/.claude/foundry.json`; `/production-audit` stamps it as `meta.owner`. Cookie check first (already signed in on the site = done), else point at the magic link. Never a blocker for using the skills.
- [x] **`/foundry list`**: built 2026-07-13 (foundry SKILL.md). No-auth local report history (project `tmp/audit-*.json` + published `reports/*.json` filtered to owner). The web view is the only surface that needs sign-in.
- [x] **Supabase magic link** (Phase 12; merged PRs #20-#21, live; see Launch sequence).

### Next product pass (from the external audit, 2026-07-12): Done in Phase 12.9

All four items landed on branch `pre-public-hardening` (2026-07-13); one
remainder from the presentation-split idea stays open below.

- [ ] **[Skill]** Move `/document`'s prescriptive presentation specifics (layout, quotes, typography, glyphs) into a Foundry template/theme; the skill prescribes information architecture + narrative, not the exact visual treatment. (The rest of the doc-drift authority item shipped in 12.5; this presentation split is the surviving remainder.)
- [x] **[Skill]** Gate applicability + scoped-audit semantics: release policy in PRODUCT.md, `/foundry check` resolves applicability, scoped audits report scoped posture not release verdicts, check vs prepare de-duplicated, homepage/deck ship-gate law reframed to policy-resolved (deliberate softening, recorded in NOTES).
- [x] **[Skill]** production-audit method depth: severity rubric broadened with catastrophic classes named; evidence classes split (runtime-reproduced / code-traced / configuration-confirmed / high-confidence / needs-verification); all `~/code/foundry` paths removed (schema bundled in the skill dir); `/code-review` + `/security-review` described as optional installs.
- [x] **[Skill]** doc-drift authority model (shipped in 12.5 via the document merge) + `document` reconcile inherits it; filesystem-mirror registry rule dropped.
- [x] **[Skill]** Per-skill depth: scaffold profiles (5, incl. internal-tool) + no-default license + document-internal handoff; mobile diagnostic patterns + full verification matrix; instrumentation governance + identity model + pageviews softened; phase-plan acceptance criteria/dependencies/non-goals/status + honest terminal outcomes; resume rigor folded into phase-plan's git-reconcile rule.

### Site + brand polish (next pass, non-blocking)
- [ ] **[Design]** Open question from the 2026-07-13 critique: should the homepage BE the example report, with marketing copy as margin annotations? (The specimen hero is the first step in that direction.)
- [ ] **[Design]** Open question: medium/verdict-risk yellow (#F5CE4B) sits one hue-step from command amber (#D99A2E) and they share viewports; decide whether that rhyme is accepted or the severity ramp needs a colder mid-tone.
- [ ] **[Site]** Three near-identical hairline card grids remain on the home scroll (dimensions, principles, install details); differentiate one if the template feel bothers.
- [ ] **[Brand]** brand-voice surface matrix (Product UI / Marketing / Public docs = strict; Technical reports = terminology strict, syntax flexible; Code comments = guidance; Internal logs = not enforced). Reconcile glossary "skill" vs the homepage line "a skill is a command Claude Code runs" [live contradiction].
- [ ] **[Site]** Skills directory: compact per-card metadata (Changes code / Invocation / Produces / Applicable to). Do not add more skills yet.
- [ ] **[Site]** Reduce quote overuse in the Behind-the-Build deck (1-2, not one per slide).
- [ ] **[Debt]** Automated route-inventory test so deleted public routes cannot survive as stale static pages.
- [x] **[Site]** Move the example report's static-only boundary up near the verdict: done in Phase 12.9, the verdict stamp now carries the "Evidence base: static analysis" line via `verdict.assessedScope`.
- [x] **[Debt]** Schema-validation script: done in Phase 12.9, `scripts/validate-report-invariants.mjs` in `make validate` (blocking findings must be verified, plus scope honesty, stats accuracy, no em dashes, no personal paths).
- [ ] **[Brand]** Sweep remaining em dashes from code comments (rendered surfaces are clean; comments are not).
- [ ] **[Site]** Per-skill richer pages (production-audit currently shares the generic SKILL render).
- [x] 2026-09-22 **[Decision]** Wordmark tracking: RESOLVED, Craig's call, the Foundry lockup is treated exactly as the Horizon Foundry lockup. It now sets at 0.04em (the kit's +40) in the chrome, the deck's title slide and the share card, so the two lockups are set identically and differ only in the word they carry. DESIGN.md had claimed that parity while shipping 0.02em.
- [x] 2026-09-22 **[Decision]** RESOLVED, Craig's call: the site abides by its own anti-goals, so the glow goes and designed motion replaces it. The ambient amber glow and the bone spotlight are gone; the hero's grid became a bounded field that reads the pointer as a coordinate, snapped to the graduation, with a registration mark at the crossing. DESIGN.md's Colors paragraph no longer claims the glow and a Motion section now carries the tokens, the reduced-motion behaviour and the motion anti-goals.
- [ ] 2026-09-22 **[Bug]** `/behind` on a phone: the carousel's instruction reads "ARROW KEYS OR THE DOTS TO NAVIGATE" on a device with no keyboard, and the dot targets are about 5px tall, well under the 44px this site markets. Make the line viewport-aware and enlarge the targets, or drop the line and rely on swipe plus bigger dots. Found by the rendered critic on the mark-swap branch at 390 wide.
- [ ] 2026-09-22 **[Site]** `/behind/design-system` at 1440 runs its content to a narrower column than its own chrome (content stops around x1056, the header and footer run to x1274), so the page reads as hanging off the left of the site container. Every other route fills it. Found by the rendered critic.
- [ ] 2026-09-22 **[Design]** The neutral ramp's two darkest swatches (`ink`, `ink-raised`) sit on a ground of the same value, so they read as outlined boxes rather than colour samples. A `line-strong` hairline (shipped on the mark-swap branch) makes each chip's extent legible; making the VALUE legible needs a lighter plate under the ramp. Decide whether a colour scale whose first stop is invisible is acceptable on the page that documents the scale.

### From the 2026-09-22 impeccable audit (14/20: a11y 3, perf 2, responsive 3, theming 3, integrity 3)

Independent technical pass over ten routes, browser-driven. The mechanical detector found 0 anti-patterns and 1 advisory (the grid-field background, already the parked decision in the workstream A leftovers above).

- [ ] 2026-09-22 **[Bug]** **[P1]** The deck's dot navigation activates the WRONG slide on 7 of 9 dots. `components/SlideDeck.tsx:204` gives each 6x6px dot a `before:-inset-2.5` hit area while the dots sit 6px apart, so every dot's area overlaps its neighbour's and the later sibling wins the hit test. Verified by clicking each dot's exact centre at 1440 with a mouse and at 390 with synthesized touch: dot 2 goes to slide 3, dot 5 to slide 6, dot 8 to slide 9. Fix: a real `min-h-11 min-w-11` target around the 6px visual dot, so the hit areas cannot overlap. Also closes the WCAG 2.5.8 failure (the 24px spacing exception does not apply when the targets intersect).
- [x] 2026-09-22 **[P1]** **[A11y]** FIXED at the root rather than by dimming or brightening type: the field (grid and crosshair) now stops before the hero's copy column and only renders where the hero is two columns, so no hairline passes behind the kicker at any width. Re-measured by the same per-glyph pixel diff at 1024, 1280 and 1440, with and without reduced motion: worst case 5.48:1, zero glyph pixels below 4.5:1. The original finding: The hero kicker fails contrast under the forge glow: `text-bone-faint` at 12px measures **2.94:1** against the real rendered pixels with the pointer parked on it (3.38:1 under reduced motion), against WCAG 1.4.3's 4.5:1. The `.grid-field` hairline runs behind the glyphs at 3.27:1 on its own and the spotlight brightens it further. `text-bone-dim` measures 6.72:1 in the same test. This is the third independent signal on the hero's ambient layer (two rendered critics called it decoration, and it now measurably breaks the first line a visitor reads), so the glow decision above is no longer only aesthetic.
- [ ] 2026-09-22 **[P1]** **[A11y]** `/reports` renders no `h1` for a signed-out visitor: the `h1` at `app/reports/page.tsx:127` is inside `{email && (...)}` and line 151 is an `h2`, so the public reports index starts at `h2` with no top-level heading (WCAG 1.3.1 / 2.4.6).
- [ ] 2026-09-22 **[P1]** **[A11y]** The deck skips a heading level on slides 2 to 9: the title slide is an `h2` (`SlideDeck.tsx:95`) and every content slide an `h3` (`:134`) under the page `h1`, so advancing the deck jumps h1 to h3 (WCAG 1.3.1).
- [ ] 2026-09-22 **[P1]** `/skills/[slug]` overflows horizontally at 320px (368px content in a 320px viewport): unbreakable raw URLs in the rendered skill doc. `.doc-prose pre` already scrolls; inline `code` and `a` were missed. Fix: `overflow-wrap: anywhere` on both in `app/globals.css` (WCAG 1.4.10 Reflow).
- [ ] 2026-09-22 **[P1]** **[Perf]** The 11 skill pages and 2 public example reports are uncached SSR on every request: no `generateStaticParams`, so `Cache-Control: private, no-store` and 12 to 39ms TTFB against 1.5ms for the prerendered routes, re-reading and re-parsing markdown per request. These are the product's main content pages and the worked-example proof the landing page links to. Content is build-time known; this is a pure win.
- [ ] 2026-09-22 **[P1]** **[Perf]** posthog-js is ~70KB gzip of a 207KB first-load bundle, eagerly imported at module scope on every route including the static marketing pages. Lazy-init behind `requestIdleCallback`, or dynamic-import it inside the existing init guard so it leaves the initial graph.
- [x] 2026-09-22 **[P1]** **[Perf]** The sticky header's `backdrop-blur` (`SiteHeader.tsx:21`) forces a GPU readback and re-blur on every scroll frame, over the nine scroll-animating `.reveal` sections that are its backdrop. DESIGN.md's Elevation section asks for depth from tonal layering and hairlines, not a blur, so the cheapest fix is also the one the design record wants: drop the blur and make `bg-ink/85` opaque. (closed 2026-09-22: shipped in PR #29. The header is opaque `bg-ink` and no `backdrop-` utility survives in `components/`; a second audit pass confirmed nothing else picked up the cost.)
- [ ] 2026-09-22 **[Design]** Three findings from the hero critic that are not the hero's, left for their own pass: the hero terminal's `0 risks 39 improvements` line colours the `0` green while the `39`, which has a severity meaning, gets nothing, so green there reads as "good news" rather than as a verdict (colour both by severity or neither); at 1024 the kicker wraps to orphan "DELIVER." alone on the second line (make the triad unbreakable so the wrap can only fall after "SUITE ·"); and at 390 the two stacked CTAs are content-width and differ by 12px, which reads as a near-miss rather than a choice.
- [ ] 2026-09-22 **[P2]** A rollup of the audit's P2 findings, none blocking. TWO ARE CLOSED by PR #29 and are kept here for the record rather than deleted: `.hero-spotlight` transitioned a gradient (a paint over the whole hero) and is gone with the element, and `HeroForge`'s synchronous layout per pointermove is fixed (20 pointermoves now produce 1 rect read), though the second audit found the cost traded to a per-scroll read, which is its own entry below. Still open: IBM Plex Serif (46KB) is preloaded on routes that never set it; `SlideDeck` animates `width` via `transition-all` on the dots; the deck has no touch gesture support at all (vertical swipe correctly passes through to page scroll, and an interrupted gesture leaves nothing stuck, both verified); `/unlock` discards the typed email and drops focus to `body` on an error; `aria-disabled` severity buttons in `ReportScorecard` stay focusable and their handler still runs, scrolling nowhere; and touch targets under 44px on the Behind tabs, the footer links, and several report controls (all clear WCAG 2.5.8's 24px, so this is the site's own standard, not AA).
- [ ] 2026-09-22 **[Design]** Two design-record gaps the audit surfaced. First, `/unlock`'s form error borrows the severity token `critical` (`app/unlock/page.tsx:93`), which DESIGN.md licenses only inside an element framed as a report excerpt; the real gap is that the token set has no error role, so the nearest severity token got borrowed. Second, motion never entered the token system: `app/globals.css` carries about 14 raw duration literals and `--ease-out-quint` sits in a bare `:root` rather than `@theme`, so no utility can reach it, and DESIGN.md has no Motion section at all. The workspace design-system policy requires one, with named usage-tagged tokens rather than ranges.
- [ ] 2026-09-22 **[P3]** Smaller items with evidence in the audit: reduced motion does not reset `animation-delay`, so staggered children still pop in sequence (CLOSED 2026-09-22 in unit A, which added `animation-delay: 0s !important` and did not say so; confirmed in the built CSS, and the review pass caught the entry still claiming otherwise); two unlabelled `<nav>` landmarks (`SiteFooter`, `BehindNav`); a redundant `role="img"` on an `aria-hidden` glyph; no `color-scheme: dark` anywhere, so native controls, scrollbars and autofill render light against the steel ground; `Terminal` schedules a timer and a render per character; and `lib/docs.ts`'s dynamic `path.join` pulls the whole project root into `.next/standalone` (build size only, both its routes are prerendered). The Dockerfile's comment claiming NOTES/PROMPTS/FRICTION are read at runtime contradicts `lib/docs.ts`'s allowlist and is stale.

### From the 2026-09-22 second impeccable audit (13/20: a11y 3, perf 2, responsive 3, theming 3, integrity 2)

Run after the hero shipped. It closed two findings from the first audit (the kicker's contrast, now 5.66:1 measured against real pixels, and the header's backdrop-blur) and cost a point: the motion layer shipped half-wired and the design record now makes three claims the code disproves. Most of what follows is mine, from today.

**Mine, introduced by the hero change:**

- [x] 2026-09-22 **[P1]** **[Bug]** DESIGN.md's Motion section says the tokens are reachable as Tailwind utilities (`duration-quick`, `ease-entrance`). Tailwind v4 resolves `duration-*` against the `--transition-duration-*` namespace, so `class="duration-quick"` emits nothing, silently, and the built chunk proves it: `.ease-entrance` exists as a rule, `.duration-quick` does not. The design record is instructing the next agent to write a dead class. Fix: rename the four duration tokens to `--transition-duration-*` in `@theme` (utilities then work and `var(--transition-duration-quick)` still resolves in CSS), or correct the sentence. (CLOSED 2026-09-22, unit A: the four duration tokens are now `--transition-duration-*` in `@theme`, so `duration-quick` emits a real rule and `var(--transition-duration-quick)` still resolves in plain CSS. Verified by building a probe page that uses the class and watching the rule appear, because the earlier "it exists" reading was Tailwind generating it from this entry's own prose.)
- [x] 2026-09-22 **[P1]** The motion tokens reach three CSS rules while 51 JSX transitions plus `duration-300` still run at Tailwind's untokenized 150ms default, which matches no token. `--duration-quick` (220ms) is documented for exactly that job, and DESIGN.md says "a raw millisecond value in a component is drift". One line fixes all 51 without touching a component: `--default-transition-duration: var(--duration-quick);` in `@theme`. Remaining raw literals after that: `.stagger-rise` 0.5s, `.draw-rule` 0.6s (which `--duration-draw` was named for, 600ms), `.text-command` 0.18s twice, `.glyph-draw` 0.9s plus a raw copy of `--ease-entrance`'s curve. (CLOSED 2026-09-22, unit A: `--default-transition-duration: var(--transition-duration-quick)` retargets all 51 without touching a component. The named literals are tokens now; `.stagger-rise` 500ms and `.glyph-draw` 900ms match no token's job and are left alone with a comment saying so, because a token is matched by what the motion does and never by the nearest number.)
- [x] 2026-09-22 **[P1]** The stylesheet contradicts DESIGN.md's own new reduced-motion paragraph. The doc says reduced motion "drops spatial movement and keeps the meaning. It is not an off switch"; the CSS sets `transition-duration: 0.001ms !important` on `*` and `transition: none !important` with `transform: none !important` on `.lift`/`.press`, so the press acknowledgement is removed with nothing substituted and colour-only transitions, which carry no spatial movement at all, are switched off. Fix: scope the blanket rule to spatial properties and give `.press:active` a non-spatial acknowledgement, or amend the doc to say reduced motion is a full stop here. (CLOSED 2026-09-22, unit A, taking the doc's side: the blanket kill is scoped to spatial properties, so colour and border transitions still read, and `.press:active` acknowledges with a border change instead of a movement.)
- [x] 2026-09-22 **[P1]** The hero band's lattice still carries the radial mask from its previous full-bleed placement, so it dissolves before reaching the band's bottom edge, which is the edge the design record claims lands on the panel. Measured down the band at 1440: the lattice is drawn for the top ~60px and has faded to the ground for the remaining ~100px, so a reading taken in the lower two thirds snaps to a graduation that is not drawn. That is the exact failure the second rendered critic named before the band was anchored, reproduced at band scale. Fix: drop the mask; the band is bounded by the panel now and needs no fade to end. (CLOSED 2026-09-22, unit B: mask dropped. Re-measured down the band at 1440, the brightest pixel per row now holds at 203 the whole way down where it previously fell to 88 by y=70.)
- [x] 2026-09-22 **[P2]** `HeroForge` traded a per-pointermove forced layout for a per-scroll one: `onViewportChange` calls `getBoundingClientRect` synchronously inside a `scroll` listener, outside the rAF the file's own comment says exists to prevent this (12 reads measured for 12 wheel ticks). The old cost was opt-in (hover); this one is paid by every home-page visit, while the page runs scroll-timeline animations that keep style dirty. Fix: set a stale flag in the handler and re-measure at the top of `apply()`. (CLOSED 2026-09-22, unit B: the handler sets a stale flag and `apply()` re-measures inside the rAF. Re-measured with 20 scroll events dispatched in one task: 0 synchronous reads, 1 read in the following frame.)
- [x] 2026-09-22 **[P2]** `HeroForge`'s gate omits the breakpoint that decides whether the band exists. Below 1024 the band is `display: none`, yet the scroll and resize listeners still attach, and the degenerate zero rect makes `column = 0`, so the component writes `--cx: NaNpx`. Fix: mirror `(min-width: 64rem)` in the media-query gate and guard `apply()` on a non-zero rect. (CLOSED 2026-09-22, unit B: the component mirrors `(min-width: 64rem)` and `apply()` guards on a non-zero rect, so below the breakpoint no listener attaches and nothing is written.)
- [x] 2026-09-22 **[P2]** The band is a `.stagger-rise` child, so it runs two opposing entrance animations: the container rises +10px while its own lattice rises -8px over an overlapping window, and a decorative element took the terminal panel's stagger slot. Fix: exclude `.hero-field` from the stagger; `grid-raise` is its declared entrance. (CLOSED 2026-09-22, unit B: `.hero-field` is excluded from the stagger and runs `grid-raise` alone.)
- [x] 2026-09-22 **[P3]** `onEnter` sets `data-live="true"` before any coordinate exists, so entering the hero anywhere lights the crosshair at the previous reading's coordinates for a frame or two. Unreachable through real input today only because a `pointermove` follows in the same task and because the scroll listener re-runs `apply()`, and the scroll fix above removes the second guard. Fix in the same change: `onEnter` seeds the coordinate and schedules; `apply()` owns the flag. (CLOSED 2026-09-22, unit B: `onEnter` seeds the coordinate and schedules; `apply()` owns the flag. Landed in the same change as the scroll fix, which removed the accident that was masking it.)
- [x] 2026-09-22 **[P3]** Under `forced-colors: active` the lattice's background-image is dropped while the tick's borders survive, leaving a lone registration corner in empty white with nothing to measure against. Fix: hide the band in forced colours; it is decoration and its scale cannot render. (CLOSED 2026-09-22, unit B: the band is hidden in forced colours.)
- [x] 2026-09-22 **[P3]** Dead declaration: `.grid-crosshair` has `z-index: 1` immediately followed by `z-index: 0`. Leftover from an aborted edit. (CLOSED 2026-09-22, unit B.)
- [x] 2026-09-22 **[P3]** `apply()` writes both custom properties every frame even though snapping leaves the value unchanged for 55 of every 56 pixels of travel. (CLOSED 2026-09-22, unit B: both properties are written only when the snapped value changes.)

**Found by this pass, not mine:**

- [ ] 2026-09-22 **[P1]** **[A11y]** Content is lost at 175% text zoom and above, at 1024 and 390 (WCAG 1.4.4 Resize Text, AA). At 1024 and 200% text, 165px of the terminal panel is clipped by the section's `overflow-hidden` and 121px of that cannot be scrolled to; at 390 the h1 loses "shippable" mid-word. Cause is the hero grid's `1.05fr`/`0.95fr` tracks resolving as `minmax(auto, …)`, so neither column can shrink below the monospace terminal's min-content. Holds through 150%; breaks at 175%. Fix: `minmax(0, …)` on both tracks and let the terminal scroll inside its own box; the header nav also needs to collapse on available width rather than at a fixed breakpoint.
- [ ] 2026-09-22 **[P2]** Several touch targets are under **24px**, which fails WCAG 2.2's 2.5.8 Target Size (Minimum) at AA rather than only the site's own 44px standard: footer links at 16px tall, the footer wordmark at 20px, the deck's Prev/Next at 50x16 and its dots at 6x6. This sharpens the earlier entry, which treated them all as an own-standard gap.
- [ ] 2026-09-22 **[P3]** Both header `<nav>` landmarks carry the identical label "Primary", so a screen reader's landmark list shows two identical entries even though only one renders at a given width. The footer `nav` is still unlabelled.
- [x] 2026-09-22 **[P1]** **[Bug]** Tailwind has no `@source` directive, so it scans the whole repo including prose, and the shipped stylesheet carries classes nothing renders: `.backdrop-blur` and `.bg-ink/85` regenerated from CHANGELOG and TODOS describing their removal, `.grayscale` from PROMPTS, and `.text-red-600`, `.animate-spin`, `.border-2` from a published audit report that quotes ANOTHER product's Tailwind classes. So a numbered default-palette colour ships in production CSS, which DESIGN.md's monochrome rule forbids, while source stays clean, which means a source-only sweep cannot see it. Fix: `@import "tailwindcss" source(none);` plus explicit `@source` for `app/` and `components/`. (CLOSED 2026-09-22, unit C: `source(none)` plus explicit `@source` for `app/` and `components/`. 18 distinct selectors across 20 rule blocks left the emitted stylesheet, each checked against a real use in `app/` and `components/` first and none found (the first count of 16 was wrong and the review pass reproduced the real number by isolating the scoping change alone). The class of defect is now asserted rather than swept: the built-output gate fails `make validate` on a numbered palette class in the emitted CSS, watched failing on an injected `.text-red-600` before being trusted.)
- [x] 2026-09-22 **[P2]** `.text-command` carries a hover glow the anti-goals ban: `text-shadow: 0 0 14px` amber, animated as a paint property, making it the only paint-driven animation left after the hero rewrite removed two for that exact reason. It is live on `/skills` (12 of 14 command tokens are inside a link or group) and dead on `/` (16 tokens, none reachable), so it is simultaneously a violation and a broken promise. Fix: delete the text-shadow, keep the colour warming, which is the brand's luminance rule. (CLOSED 2026-09-22, unit C: text-shadow deleted, colour warming kept.)
- [x] 2026-09-22 **[Design]** Motion has no machine-readable record and no rendered surface: DESIGN.md's frontmatter carries `colors`, `typography` and `spacing` but no `motion`, so the mirror `app/globals.css` claims ("tokens mirror DESIGN.md frontmatter") is incomplete and a mechanical check cannot see the motion layer; and `/behind/design-system` documents colour and type but not the new tokens. Fix both in one pass. (CLOSED 2026-09-22, unit A: a `motion:` block in DESIGN.md's frontmatter and a motion row-set on `/behind/design-system`.)

**From the confirm critic on the fixed hero (2026-09-22, third pass):**

Given only the after-screenshots at 1440 and 390 and DESIGN.md's anti-goals, no diff and no conversation. Its central finding is a decision, not a defect, and it is the strongest version of an argument two earlier passes made and this session argued against, so it is recorded as Craig's call rather than closed.

- [x] 2026-09-22 **[Decision]** **The measuring band measures nothing, and the critic says that makes the snap decoration in better clothes.** The argument, which the previous defence ("snapping to a graduation is a measurement, not an ambient wash") does not answer: nine columns by three rows graduate no quantity on the page, there is no axis, no label and no value, so the crosshair reports the pointer's own position back to the pointer. Its supporting evidence is the part that lands: the band is dropped entirely below 1024 with nothing lost, and an element that can vanish on a phone with no loss carries no information at 1440 either. Two fixes offered. Cut the band and the crosshair, and let the terminal card rise so its top edge starts on the kicker's cap line, which leaves the hero with no motion and reopens the ask this wave exists to answer. Or make it measure the run underneath it: plot the audit's own dimensions along the columns, label one axis, and step the crosshair per output line so its position means "this is the dimension being read", stopping when the verdict lands. The second is the larger build and is the one that turns the objection into the feature; it also gives the motion something static copy already carries, which the motion anti-goals require. **(RESOLVED 2026-09-23, Craig's call: cut, not redesigned.** Two reasons no amount of craft answers. A crosshair is a gunsight, and that association is not one this brand should carry whatever the geometry means, which is an argument about the mark rather than about the measurement and which no critic had made. And it existed only above 64rem in one corner of one page: an element that vanishes on a phone with nothing lost was carrying nothing at 1440 either, which is exactly what three rendered critics kept saying. The band, the lattice, the crosshair, the registration tick and `HeroForge` are all gone; the hero's motion is the terminal typing a real command and landing on a verdict, which carries the same claim the copy beside it makes. DESIGN.md's Motion section records the decision and its lesson: an element that has to be argued for is usually decoration with a good lawyer.)**
- [ ] 2026-09-22 **[Design]** The GITHUB button is the only rounded element on the page (about 4px). Within DESIGN.md's stated 2 to 4px range, so not a violation, but every other edge on the hero is square-cut, which makes the one radius read as an untouched default rather than a choice. Square it or state the exception.
- [ ] 2026-09-22 **[Design]** The terminal panel's three macOS window dots do nothing: they are not controls, the panel is not a window, and the "SIMULATED RUN" label above already does the disclosure they imply. Cut-pass candidate on the next hero change.
- [ ] 2026-09-22 **[Debt]** **The mirror claim is still unenforced, which is this wave's own lesson turned back on it.** `app/globals.css` says its `@theme` tokens mirror DESIGN.md's frontmatter, and `make validate` has no check that compares them: `check-brand-assets.mjs` reads DESIGN.md only as a claimant file, `check-built-mark.mjs` never opens it, and nothing parses the frontmatter at all. Unit A's fix added the motion block to the frontmatter and the values do match the stylesheet today, verified by hand, which is exactly the verification unit C replaced with a gate for the palette. Fix: a `scripts/check-design-tokens.mjs` in `make validate` that parses the frontmatter and asserts every token it declares appears in `@theme` with that value, and fails on a name in one and not the other. Closes the loop for colour, type, spacing and motion in one script. Found while confirming unit A's frontmatter parses, not by an audit.

**From the adversarial pass over units A to C (2026-09-22, three fresh agents):**

Three agents, one angle each, all browser- or build-verified. They found twelve real defects in the fix wave, two of them regressions units A and B had just introduced and five that defeat the band's own thesis. All are fixed in this branch except the four logged as open below.

- [x] 2026-09-22 **[P1]** **[Bug]** `git add -A` in the docs commit swept a reviewer's in-flight experiment and silently reverted unit C's `source(none)` scoping, so the branch tip shipped unscoped Tailwind, shipped `.text-red-600` again, and failed its own new gate in CI. Two agents found it independently, and the one that caused it deliberately left the file modified rather than running `git checkout --`, so that a blind `git add -A` would restore correctness instead of cementing the regression. Fixed by commit. The lesson is in the process, not the code: never `git add -A` while a sub-agent with write access is mid-experiment on tracked files, and stage explicit paths when one is running.
- [x] 2026-09-22 **[P1]** **[A11y]** Unit A's reduced-motion press acknowledgement erased the word it was acknowledging. `.press:active { background-color: var(--color-ink-raised) }` applied globally put `#14191f` text on `#1e2530` at **1.15:1** on both bone CTAs (the install button and the `/unlock` submit), for reduced-motion users only, and the border half of the same rule was a no-op because those buttons already sit at `--color-line-strong`. Fixed: the border takes the command amber, which is a real change on both plate buttons and cannot touch the label. Re-measured in a browser under `reducedMotion: reduce` with a real mouse-down: label contrast **14.88:1 idle and 14.88:1 pressed**, border `rgb(69,86,106)` to `rgb(217,154,46)`, transform `none`.
- [x] 2026-09-22 **[P1]** **[Bug]** The crosshair latched on. `onLeave` set `data-live="false"` and neither cancelled the pending frame nor set anything `apply()` could read, so the frame recomputed `live` from coordinates still inside the band and turned it back on, leaving the crosshair lit with no pointer on the page until the next scroll. Deterministic on a touch laptop (every tap fires enter, down, up and leave in one task) and intermittent on a fast mouse exit. This was unit B's own change: it moved the flag into `apply()` so the enter could not assert it, and put it somewhere the leave could not reach. Fixed with an `inside` flag that the leave clears and the frame respects, plus cancelling the frame. Verified: enter-move-leave in one task and enter-leave with no move both end `live=false`, `opacity=0`.
- [x] 2026-09-22 **[P1]** **[Bug]** One resize across 64rem and back killed the crosshair permanently. The last `onViewportChange` before the crossing cached the `display: none` band's ZERO rect and cleared the stale flag, so every later frame hit the zero-rect guard and returned. Any window maximise, OS snap, display change or devtools toggle is one such crossing. Fixed by deleting the rect cache: `apply()` measures inside the rAF every frame, which also fixed the finding below and made the component smaller. A second latch surfaced during verification and is fixed too: after re-attach no `pointerenter` is coming for a pointer that never left, so `inside` is set by the move as well as the enter. Verified 1440 to 900 to 1440 with a plain move and no re-entry: `live=true`.
- [x] 2026-09-22 **[P1]** **[A11y]** The reading came off the lattice at any non-default browser font size. `ROW = 56` was hardcoded in JS against `3.5rem` in CSS, and the two agree only at a 16px root; at Chrome's "Large" (20px) the band held three 70px rows while the snap still stepped by 56, putting the reading up to 28px from a drawn graduation, with the horizontal ruled through the middle of a cell. The file's own comment warned about exactly this drift while causing it. Fixed by deriving the row from the measured box (`height / ROWS`), so CSS owns the pixels and there is one source of truth. Verified at a 20px root: drawn pitch 70px, every reading an exact multiple, 0px off.
- [x] 2026-09-22 **[Bug]** The reading vanished at the band's far edges, and the first fix for it was wrong in a way the second review round caught. Rounding admits one index past the last lattice line on each axis, and `.hero-field`'s `overflow: hidden` clipped the crosshair's own 1px line there, so it disappeared in the right 5.6% and the bottom 17% of the band, the bottom strip being where a pointer heading for the terminal sits. The first fix clamped the snap to the second-to-last graduation, which treated a clipping problem as a range problem and cost the instrument its meaning: measured zones became 28 / 56 / 84px, so the bottom HALF of the band all reported one value, and a pointer 1px above the panel's top border had its reading drawn 56px away from a line that was right there. The real point is that those two indices are real graduations: the last column edge IS the panel's right border and the last row IS its top border, in the same `--color-line` the lattice uses, which the CSS already claimed in so many words. So the clamp is gone and the band is no longer clipped (the lattice cannot overflow anyway, being a background on an inset-0 child). Re-measured: zones 28 / 56 / 56 / 28, four graduations reachable. **And then the third review round showed that unclipping was itself wrong, in five ways, so the final shape is different again.** The argument for removing the clip ("a background on an inset-0 child cannot overflow") is false: `.grid-field` is TRANSFORMED by `grid-raise`, so unclipped the lattice fringed up to 8px above the band for the first ~200ms of every default-motion load, measured as a full-width hairline 3px above it at t=120ms. The registration tick hung outside every corner, 33px of paint at the origin and 444px at the far one, which falsified DESIGN.md's "bounded to the field (a reading off the scale is not a reading)". A `--cx` held from a wider layout painted 39px outside for the 120ms fade after a resize. The vertical line at index 9 landed 1px OUTSIDE the panel's right border rather than on it, a border-box off-by-one that recreated the near-miss the band's placement exists to avoid. And the horizontal line at index 3 was invisible: it landed exactly on the panel's top border, and the panel is a stacking context via its own entrance transform, so it painted over it. That last one is the same error as this entry's own first claim: the re-verification checked `--cy` and `getBoundingClientRect`, which neither clipping nor occlusion affects, and never sampled a pixel. Final shape: the band is clipped again, the snap INDEX stays unclamped so the zones stay uniform, and the DRAWN position is clamped to the band's last pixel. Verified by sampling pixels at 1024, 1280 and 1440: index 3 paints `69,86,106` one row above the panel border, index 9 paints on the border itself, zero instrument pixels outside the band at the origin, zero lattice pixels above the band at `grid-raise` t=120ms, and zero crosshair pixels outside after a 1440-to-1100 resize. The two axes differ by a pixel for a border-box reason now stated in the code: the panel's right border occupies [R-1, R] so a line at width-1 is exactly it, while its top border occupies [T, T+1] where T is the band's bottom edge, so a line at height-1 sits directly above and the two read as one 2px rule.
- [x] 2026-09-22 **[Perf]** **[Site]** The `@tailwindcss/typography` plugin was dead and was 19% of the stylesheet. Nothing uses `prose` or any `prose-*` utility (the only real class is the hand-written `doc-prose`); the whole block was being generated from the English word "prose" in two page strings. It also carried the one shadow left in the build, a `kbd` `box-shadow` that DESIGN.md bans outright. Plugin removed: the emitted stylesheet dropped **12,002 bytes** (70,685 to 58,683, measured before and after that single change), `.prose` and `tw-prose-kbd` are gone, and the only remaining `box-shadow` in the build is preflight's `box-shadow: none`. Independently confirmed by recompiling with the plugin restored: 76 rules appear, 0 disappear, 0 change a declaration, and all 76 are anchored on `.prose` or `.not-prose`, so removing it cannot reach an element without a `prose` class.
- [x] 2026-09-22 **[Bug]** `lib/` was outside the new source list while being a real class-name source: `lib/report-types.ts` holds full class strings with a comment saying the scanner is the only thing keeping them alive. All four survived only because each happens to be duplicated in `app/` or `components/`, which is luck; the next one added without a duplicate would ship unstyled on `/reports/[slug]` and `/example/[slug]`. Fixed: `@source "../lib"`.
- [x] 2026-09-22 **[Bug]** The new palette gate was defeated by 13 of 16 real emitted forms, because a palette class is only bare in the simplest case: every variant prefixes it (`.hover\:text-red-600`, `.sm\:…` inside a media query, arbitrary variants) and several utilities infix it (`.border-t-red-600`, `.ring-offset-red-500`). All of them resolve to `var(--color-<palette>-<n>)`, so the gate now asserts on the variable as well as the class name, and keeps the class pattern only because it names the offending selector in the failure message.
- [x] 2026-09-22 The retarget reached 50 of 51, not 51, and the miss was the one raw millisecond value in a component: `Terminal.tsx`'s `duration-300` on the output reveal, which the finding text itself had named while the closure claimed otherwise. Its job is "an element arriving", so it is `duration-settle` now, which also gives the DESIGN.md claim that the utilities work its first real consumer. Verified in a browser: bare `transition-colors` 0.22s, `duration-settle` 0.42s.
- [x] 2026-09-22 Three sibling misses the same commit created: `.text-command` kept a blanket `transition: none !important` under reduced motion although that commit had just made it colour-only, which is exactly what the preference keeps; `.draw-rule` and `.stagger-rise` shared an undeclared THIRD easing while the commit was converting a hand-copied curve to a token nine lines away (now declared as `--ease-draw`, the entrance whose end point is fixed, where overshoot would read as a bounce); and the `.hero-field` stagger exclusion was on the animation but not the delays, so the band still spent the first slot and the panel arrived at 100ms instead of 40ms. (That last one was fixed WRONG first, and the second review round caught it: `:nth-child()` counts all children regardless of `:not()`, so `:not(.hero-field):nth-child(1)` matched nothing in the hero's container and the panel still arrived at 100ms while this entry claimed 40ms. A decoration should not have been a child of a stagger container at all; the band is out of it now, every exclusion is deleted rather than made cleverer, and the measured delays are 40ms on the panel and 40/100/160/220ms on the copy column.)
- [x] 2026-09-22 Two doc-truth repairs and two record gaps: DESIGN.md's Asymmetry paragraph and the `@theme` comment still named the pre-rename tokens, so a reader following either wrote a name that resolves to nothing; and the `motion:` frontmatter and the `/behind/design-system` row-set both listed the tokens while omitting `--default-transition-duration`, the one declaration that governs 50 of the 51 transitions. Also fixed: the band was a hit-test target at all four corners despite being `aria-hidden`, now `pointer-events: none`.
- [ ] 2026-09-22 **[Debt]** The `--animate-*` half of the motion theme was never swept. `Terminal.tsx`'s `animate-pulse` resolves to `pulse 2s cubic-bezier(.4, 0, .6, 1) infinite`: 2s matches no token and the curve matches no easing, yet `--animate-*` is a first-class theme namespace exactly like `--transition-duration-*`. Not the "nothing pulses forever" anti-goal (the cursor unmounts after about a second), so this is a token-coverage gap rather than a violation, and it is the obvious next unit for the motion system.
- [ ] 2026-09-22 **[Debt]** **Prose-generated CSS is smaller, not zero, and the claim that it was zero did not survive the next review round.** What is true: the wave took the emitted stylesheet from 70,685 bytes to about 56,000 at the tip, and it removed every case that mattered, including the numbered palette class the monochrome rule forbids. (Deliberately "about": the exact figure moved four times across this branch's commits as each one added or removed a rule, so a precise byte count asserted in prose is stale by the next commit. The measurement belongs in the PR's evidence, where it is dated to a commit, not in a doc that outlives it. This entry previously carried three different exact numbers in three places, none of which matched the tip.) What was false, and it was checked against a list of sixteen names rather than against the artifact, which is the same instance-not-invariant error this wave keeps finding: **twelve utilities still ship that appear in no `className` anywhere.** Three of them are `.ease-entrance`, `.ease-exit` and `.ease-draw`, minted from the `<code>` tags on `/behind/design-system` that this wave added to DOCUMENT those tokens, which is the `.blur` defect verbatim in copy written by the same wave. The rest are `.h-7`, `.h-8`, `.resize`, `.invisible`, `.container`, `.fixed`, `.static`, `.table`, `.visible`. And `.resize` settles the strategy question: it is minted partly from `window.addEventListener("resize", ...)`, which is live code and cannot be reworded, so the "do not name the utility in the comment" workaround used for `.blur` closes single cases and can never close the mechanism. Stop reaching for it. The real fix is the one this wave applied to the palette: assert it. A gate can extract every class token from the built stylesheet and fail on any that appears in no `className` and no hand-written selector, which is exactly the sweep the reviewer ran by hand. It would fail on twelve today, so it lands with those twelve fixed or explicitly allowlisted, and it is the natural sibling of the unenforced-mirror gate logged above. Both belong in one unit.
- [ ] 2026-09-22 **[A11y]** The landing page's primary CTA has never acknowledged a keyboard press, under either motion preference. `app/page.tsx:138` is an `<a>`, and Chrome does not match `:active` on Enter for links, so both the transform and the reduced-motion border change are unreachable from the keyboard: measured `border: rgb(69,86,106)`, `transform: none` on keyboard activation in both cases. Pre-existing and not introduced by the motion work, but it is the install button, so it is the one press on the site most worth confirming.
- [ ] 2026-09-22 **[Debt]** Stated gap in the palette gate, deliberately not papered over: a hand-written `#dc2626` or `oklch(...)` is the same monochrome violation with no class and no variable to find, so nothing catches it. Closing it means enumerating the brand's own values and failing on anything else, which is a larger check than this one.
- [x] 2026-09-22 Two more from the second round, both cheap: the component measured the band once per frame while hovering in `forced-colors: active`, a mode where the band is `display: none` and can never render (it now mirrors that media query as a fourth gate, verified 0 measurements for 20 moves); and `inside` survived a `pointercancel`, which the component did not listen for, so a scripted cancel left the crosshair lit until the next scroll (a real cancel is followed by `pointerleave` and self-healed, so this was latent). `pointer-events: none` on the band also structurally closes the remaining synthetic-move latch path, since a subtree that cannot be a hit target cannot be removed mid-gesture under the pointer.
- [x] 2026-09-22 **[A11y]** At a root font size below 16px the band's top rows fall outside the hero section and are both undrawn and unreachable. Measured: at 12px, 43.09px of the band (34%) is clipped by the section's `overflow-hidden` and the painted lattice lines are at band y 42 and 84 with none at 0; at 14px, 24% is clipped and the row-0 reading is unreachable because the listeners are on the section a pointer there never enters. Same family as the far-edge clipping above, at the opposite edge, and pre-existing rather than introduced. Rides with whatever the band decision resolves to, since a cut band makes it moot. (VOID 2026-09-23: the band is gone, so there are no rows to clip.)
- [x] 2026-09-22 **[Design]** For about half a second on load, the lattice, the band box and the panel border are in three different places: the panel animates `translateY(10px)` to 0 under the stagger while `.hero-field` is positioned against the un-animated container, and `.grid-field` is simultaneously offset by its own `grid-raise`. Measured at 1440: a 4.03px gap at t=200ms, flush from t=700ms. Only visible with the pointer over the band on load. Rides with whatever the band decision above resolves to. (VOID 2026-09-23: the band is gone, so there is nothing to misalign against the panel.)
### Next proof pass (post-launch credibility)
- [ ] **[Proof]** Publish 2+ more example audits across meaningfully different app types (not just a web SaaS). Show repeatability across independent runs on the same commit. Publish approximate repo size, audit duration, model config per run. Demonstrate one complete before-and-after: `/foundry check` -> `/foundry prepare` -> final `/production-audit`. Eventually publish known false positives and how the verifier eliminated them. Synthetic / permissioned targets only.

### Brand / infra
- [x] 2026-09-02 **[Infra]** **Create the Supabase keep-alive target.** In the Supabase dashboard SQL editor for this project, run `create function public.ping() returns timestamptz language sql as $$ select now() $$;` and `grant execute on function public.ping() to anon;`. Until this runs, `/api/health` returns 503 and the daily keep-alive job fails, deliberately: the endpoint reports healthy only when Postgres returns a value, so a missing function is loud rather than silently green (the exact failure this replaced). If the project is currently paused, restore it first. Craig does this in the Supabase dashboard. See NOTES 2026-09-02. (closed 2026-09-17: the project had in fact been paused the whole time, which the rewritten endpoint proved by reporting a transport failure where the old one had reported success. Craig restored it and ran the DDL; `/api/health` returns ok and the daily keep-alive job is green on both legs for the first time since 2026-09-02.)
- [ ] **[Infra]** Destroy the old `production-audit` Fly app: stopped and scaled to 0 since the Foundry rename, and the replacement URL is confirmed live. (Relocated from CLAUDE.md, 2026-07-29 context-rightsizing pass.)
- [ ] **[Brand]** Point a custom domain (e.g. foundry.horizonfoundry.com) at the Fly app once the domain is set up.
- [x] **[Brand]** Rename the Fly app to a Foundry URL (foundry-skills.fly.dev)
- [x] **[Docs]** Public install path (npx skills add + from-source make install)

## Done this session (2026-07-12): see PROMPTS Phases 6-11 for detail
- [x] Removed /commit from the public suite; rebuilt privately as /pr (stop at PR) and /ship (merge + deploy); global CLAUDE.md points at them
- [x] Renamed behind-the-build -> document; added document public/internal/reconcile modes; curated the public /behind hub (raw logs removed)
- [x] Added foundry check (read-only) / prepare (modifying); verdict is an independent production-audit
- [x] Safety pass: real report swapped for the synthetic Perch audit; docs de-personalized
- [x] Adoption: install section (npx-first), nav restructure, CTA, CONTRIBUTING, CHANGELOG, version/license surfaced
- [x] Added shipGates to the report contract (schemaVersion 2), rendered under the verdict; unified the verification contract; scrubbed rendered em dashes; fixed the "fictional sample" provenance
